{
  "name": "prueba-cypress",
  "version": "1.0.0",
  "description": "prueba cypress",
  "main": "index.js",
  "scripts": {
    "test": "test",
    "cy:open": "cypress open",
    "cy:runChrome": "cypress run --browser chrome --spec 'cypress/e2e/**/*.cy.js'",
    "build": "if exist dist rmdir /S /Q dist && mkdir dist && xcopy src dist /E /Y"
  },
  "keywords": [
    "e2e",
    "cypress",
    "automation",
    "framework"
  ],
  "author": "yanca",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^14.4.1",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^5.0.0",
    "mochawesome-report-generator": "^6.2.0"
  }
}
